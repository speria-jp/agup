{
  "name": "agup",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '@agup/cli' dev",
    "build": "bun run --filter '@agup/cli' build",
    "test": "bun run --filter '*' test",
    "test:e2e": "bun run --filter '@agup/cli' test:e2e",
    "lint": "bunx oxlint packages/",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "lefthook": "^2.1.6",
    "oxlint": "^1.61.0",
    "typescript": "^6.0.3"
  }
}
